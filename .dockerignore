<<<<<<< HEAD
﻿# Git
.git
.gitignore
.dockerignore
.github/
.gitattributes

# IDE/OS
.idea/
.vscode/
*.swp
.DS_Store

# Node / pnpm
node_modules/
.pnpm-store/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
env/
venv/

# Logs / test / dev
*.log
logs/
test-results/
frontend/test-results/
pytest*.log

# Runtime data (volumes in prod)
# NOTE: we exclude generated runtime files, but seed data files must be available
# for the Dockerfile COPY step. Use negation to keep only the small seed files.
media/
data/
!data/cfg1.json
!data/projects.jsonl
chroma_db/

# Dev builds (Docker rebuilds them)
frontend/node_modules/
artifacts/**/node_modules/
frontend/dist/
artifacts/concierge/dist/

# ── Secrets (critical - never bake real env/secrets into image) ──────────────
# Explicitly ignore ALL environment and secret files so no keys ever enter the image.
.env
.env.*
.env.local
.env.*.local
!.env.example

*.pem
*.key
*.crt
*.p12
*.jks
secrets/
*.secret
*.token
*.apikey
*.credentials

# Recursively ignore in all subdirectories
**/.env
**/.env.*
**/.env.local
**/.env.*.local
**/secrets/
**/*.pem
**/*.key
**/*.token

# ── Additional bloat reduction for faster builds ─────────────────────────────
# Git and CI
.git/
.gitignore
.github/
.gitattributes

# Docs and misc (keep only what's needed)
*.md
!README.md
*.txt
!requirements*.txt
!package.json
!pnpm-*.yaml
!pnpm-lock.yaml

# Test and dev only
**/__tests__/
**/*.test.*
**/*.spec.*
test-results/
coverage/

# Caches and temp
.cache/
.tmp/
*.tmp
*.bak
*.swp
*~

# Additional logs
**/*.log
**/*.log.*

# Docker self
docker-compose*.yml
Dockerfile*
.docker/
=======
# ── Version control ───────────────────────────────────────────────────────────
.git/
.gitignore
.gitattributes

# ── Docker meta-files (not needed inside the image) ──────────────────────────
docker-bake.hcl
docker-compose*.yml
Makefile
.env.example

# ── Node / pnpm ───────────────────────────────────────────────────────────────
node_modules/
**/node_modules/
# Built artefacts are produced inside Docker — never copy local builds in
artifacts/*/dist/
artifacts/*/build/
*.tsbuildinfo

# ── Other artifact packages not needed by Docker ─────────────────────────────
# Only artifacts/concierge (the web frontend) is built in the image.
# Mobile app, API server, and mockup sandbox are excluded.
artifacts/concierge-mobile/
artifacts/api-server/
artifacts/mockup-sandbox/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
**/__pycache__/
*.py[cod]
*.pyo
.pythonlibs/
.venv/
venv/
.pytest_cache/

# ── Replit-specific ───────────────────────────────────────────────────────────
.replit
replit.nix
.replitignore
.cache/
.local/
.config/
.agents/

# ── Secrets / environment files ───────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Large/sensitive runtime files ─────────────────────────────────────────────
memory_backup.jsonl
*.jsonl
.migration-backup/
media/
data/
screenshots/
attached_assets/

# ── Editor / OS artefacts ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.sublime-workspace

# ── Test artefacts ────────────────────────────────────────────────────────────
coverage/
playwright-report/
test-results/
htmlcov/

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-error.log
>>>>>>> f665b8188591020c7f82f8a93d3211e3cc2ffcb5
