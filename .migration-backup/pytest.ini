[pytest]
# Restrict test collection to the tests/ directory to avoid non-Python files
# in the repo root being picked up by plugins or accidental discovery.
testpaths = tests
python_files = test_*.py

# More verbose by default when running locally
addopts = -q
