fastapi>=0.95.0
uvicorn[standard]>=0.22.0
pydantic>=1.10.0
openai>=1.0.0
httpx>=0.23.0
# Minimal runtime requirements for Vercel deploy. Heavy/optional dependencies moved to `requirements.full.txt`.
# Optional heavy packages (chromadb, onnxruntime, celery, redis, etc.) are not installed in Vercel builds to
# keep serverless package size under limits. For full local development, install from `requirements.full.txt`.
# Phase 14: workstation file-upload layer
python-multipart>=0.0.6
Pillow>=10.0.0
python-dotenv>=1.0.0
